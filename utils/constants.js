const OWNER_ID = '1456824205545967713';

const GROUP_164    = '948951510';
const GROUP_TAGS   = '575770529';

const GROUP_164_ROLES  = ['164'];
const GROUP_TAGS_ROLES = ['KITTY TAG', 'lurk tag', 'AMOR TAG', 'YinYang'];

const ALL_TAG_NAMES = [...GROUP_164_ROLES, ...GROUP_TAGS_ROLES];

const CATEGORIES = ['all', 'vanity', 'sniper', 'tags', 'tickets', 'verify'];

const DEFAULT_PREFIX = '!';

module.exports = {
  OWNER_ID,
  GROUP_164,
  GROUP_TAGS,
  GROUP_164_ROLES,
  GROUP_TAGS_ROLES,
  ALL_TAG_NAMES,
  CATEGORIES,
  DEFAULT_PREFIX,
};
